alias s="sudo"
alias p="pacman"
alias cl="clear"
alias ta="tmux attach"
alias tc="tmux create-session"
alias tls="tmux list-sessions"
alias y="yarn"
alias cb="cmake -B build -DCMAKE_POLICY_VERSION_MINIMUM=3.5 -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DCMAKE_CXX_STANDARD=17"
alias b="bun"
alias code="$HOME/VSCode/code --ozone-platform-hint=wayland"
alias vim="nvim"
alias c="cargo"
alias get_idf='. $HOME/esp/esp-idf/export.sh'

export PATH=$PATH:/home/veaq/.spicetify:$HOME/.emacs.d/bin/:$HOME/tgs2png/build/tgs2png:/usr/local/texlive/2025/bin/x86_64-linux/

# Created by `pipx` on 2025-05-08 16:41:16
export PATH="$PATH:/home/veaq/.local/bin:$HOME/.config/emacs/bin/"
export IDF_PATH=$HOME/esp/esp-idf
export DOOMDIR="$HOME/.config/doom"
export EMACSDIR="$HOME/.config/emacs"

# Created by `pipx` on 2025-05-14 19:42:31
export GOPATH="$HOME/go"
export PATH="$PATH:/home/vqphd/.local/bin:/usr/local/texlive/2025/bin/x86_64-linux:/home/vqphd/go/bin"
export GO111MODULE=on
#THIS MUST BE AT THE END OF THE FILE FOR SDKMAN TO WORK!!!
export SDKMAN_DIR="$HOME/.sdkman"
[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"
export TERM="xterm-256color"
export COLORTERM=truecolor
export GITLAB_TOKEN="glpat-9mynzKu-GtKEqxyR4fGi"
export PATH=$PATH:/home/vqphd/.spicetify

# 🛠️ Rebuild NixOS system
alias ns='sudo nixos-rebuild switch --flake /etc/nixos#nixlab'

# 🧪 Apply Home Manager changes
alias hm='home-manager switch --flake /etc/nixos#vqphd'

# 🔁 Rebuild both (system + user)
alias nix-rebuild='ns && hm'

# 💥 Garbage collection
alias nix-gc='sudo nix-collect-garbage -d && nix-collect-garbage -d && nix store gc'

# 🔎 Diff generations
alias nix-diff='nvd diff /run/current-system /nix/var/nix/profiles/system'

# 📄 Show current system config
alias nixos-config='cd /etc/nixos && lsd --tree'

# 📁 Edit flake
alias flake-edit='nvim /etc/nixos/flake.nix'

# 🧠 Show news
alias nix-news='home-manager news'

# ⚙️ Update flake inputs
alias flake-update='cd /etc/nixos && nix flake update'


nixdo() {
  echo "Choose: [1] System, [2] Home, [3] Both"
  read -r choice
  case "$choice" in
    1) ns ;;
    2) hm ;;
    3) nix-rebuild ;;
    *) echo "Invalid" ;;
  esac
}

eval "$(zoxide init zsh)"
